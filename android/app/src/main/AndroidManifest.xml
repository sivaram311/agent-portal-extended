<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".AgentPortalApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.AgentPortal"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AgentPortal"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- CSS OAuth/PKCE redirect back into the app. Requires this scheme to be
                 added to css-next's redirect allow-list before OAuth login works end to end. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="buzz.delena.agentportal" android:host="oauth" android:path="/callback" />
            </intent-filter>
        </activity>

        <!-- Handles Approve/Reject actions from the permission-approval notification
             (PermissionApprovalNotifier). Plain local broadcast receiver, no Firebase
             dependency, safe to register now. -->
        <receiver
            android:name=".notifications.PermissionActionReceiver"
            android:exported="false" />

    </application>

</manifest>
