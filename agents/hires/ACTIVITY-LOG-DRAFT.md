# Activity-log draft (paste into `E:\MyAgent\workflow\activity\ACTIVITY-LOG.md`)

Scratch file — not the real log. The parent session should copy the row below to the top of the real `ACTIVITY-LOG.md` (below its header block) once all three subagents' work is verified on disk.

| 2026-07-26 (session) | `agent-portal-extended-skeleton-2026-07-26` | claude-code | dev+docs | Built new Android skeleton app for Agent Portal: 3 parallel same-model subagents (network/data layer, Compose UI, docs — this one) working under `E:\MyWorkspace\agent-portal-extended`. Delivered Compose UI skeleton (login, session list, chat/transcript, navy/teal branding), Retrofit/OkHttp networking against the existing Agent Portal REST API, hand-rolled STOMP-over-WebSocket client for `/ws/websocket`, Room local cache, EncryptedSharedPreferences token storage. Gradle build (JDK 21, AGP 8.11.1, Kotlin 2.3.21, Gradle 8.14) assembles clean. Zero device/emulator verification — no ADB on this build host, same disclosed limitation as `forgecity-launcher`. OAuth/PKCE SSO, FCM push, biometric app-lock, backend `device_tokens`/`WebhookService` push dispatch, and the inline notification-approval flow are explicitly deferred (tracked in the new repo's `ROADMAP.md`). New public GitHub repo `agent-portal-extended` (`sivaram311/agent-portal-extended`) — not yet pushed at draft time. Added a companion node to the `mindmap` repo's "Apps & Interfaces" branch and a new roadmap row in `agent-portal\docs\ROADMAP.md`. | `E:\MyWorkspace\agent-portal-extended` · https://github.com/sivaram311/agent-portal-extended | ok | no device verification; no OAuth/push/biometric/remote-approval yet; release not tagged at draft time — versionName/APK SHA in `docs\HANDOFF.md` still TBD |
